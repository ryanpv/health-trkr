import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from "react-native"

const Home = () => {

  return (
    <SafeAreaView>
      <Text>Home Page</Text>
    </SafeAreaView>
  )
}

export default Home;